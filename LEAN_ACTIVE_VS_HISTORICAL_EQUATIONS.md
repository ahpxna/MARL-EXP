# Active versus historical equations — clean state

Active: exact deficit identity; finite `zetaDef`; finite gauge `Delta`; BH6 global `E/2`; support brackets; finite conditional-kernel `R_iso`; TV isolation; RF1; operational Gamma; fully instantiated Operational MASTER; modularity/coextremizability equivalence on finite support; actual response-geometry strictness witnesses; functional C/D; zero-sum gauge direction and `lambdaD` sign stability; selected split contrast; continuous Neyman allocation; product-surrogate `(m-1) delta_square`; actual Chain-D compression loss and 1-Lipschitz bridge; exact full-product `A_q` modular formula; Top-C surrogate optimality; D6 literal Top-C downstream decision transfer; exact Query-E witnesses; Chain-A V6 A8–A14 two-margin package.

Historical/separate: generic absolute-zeta; raw ungauged component error; coarse `k(delta+chi)+zeta+2eta`; arbitrary-set-function W1/W3; additive-only product-surrogate identity; superseded `LeanUnifiedChainsV2.lean`. These are not silently composed into the active chain.

`R_iso`, feasible-response semantics and product-surrogate semantics remain separate typed routes. Within-relation extrema margin `g_j` and between-relation capacity margin `G^C_{jℓ}` remain separate typed concepts.
