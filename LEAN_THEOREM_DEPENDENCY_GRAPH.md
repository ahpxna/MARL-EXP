# Lean theorem dependency graph — clean active state

```text
BH1/BH2 → BH3 exact deficit
BH3 → finite global-E domination → BH6 cardinality E/2
BH3 + finite zetaDef + actual Top-C cardinality → BH5
component gauge → fixed-S stability → finite Delta → CERT

finite kernel → R_iso oscillation
probability weights → TV expectation bound → chi bound
marginal indicator basis → RF1
R_iso + operational Gamma + BH5 + downstream eta
  → frozen fully instantiated Operational MASTER

global coextremizability → all-subsets modularity
all-subsets modularity + full-set deficit + finite attainment
  → global coextremizability
all-subsets modularity → radius formula → Top-C exact

osc(Qhat-Q)/2 → A8 capacity error
A8 capacity error + between-relation margin → A10 pair order
A10 pair order + cardinality/strictness → exact Top-K membership
within-relation gap → local extrema stability
two-relation witness → local stability does not imply global rank stability
zero-sum weights + exact gauge center → |Dhat-D| ≤ ||w||_1 delta_Q^circ
zero-sum gauge bound + lambdaD < 1 → D sign stability

hybrid telescoping → pointwise (m-1) delta_square
product normalization + pointwise bound → product-surrogate D3
oscillation compression definition → proved 1-Lipschitz world bridge
D3 + proved bridge → actual compression-objective perturbation
actual perturbation + finite cardinality surrogate optimum
  → D5 true compression-decision regret
full Cartesian product → simultaneous component extrema
simultaneous extrema + additive `A_q`
  → `L_Aq(S) = sum_(j notin S) osc(Q_j^q)/2`
exact formula + Top-C cardinality property → surrogate optimality
D3 + actual bridge + derived Top-C optimality
  → D6 literal Top-C true compression-decision regret
finite unbiased evaluation mean → selected split contrast
Cauchy bound + exact candidate evaluation → continuous Neyman optimum
```

The auxiliary structural, Chain-A V6, Chain D and Query E programmes do not feed back into or alter the frozen MASTER.
