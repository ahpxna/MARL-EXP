# Lean assumptions — clean active state

- Support geometry assumes finite nonempty feasible support and finite component index type. Support inhabitants represent feasible actions, not observed rows.
- BH6 uses actual finite extrema and `globalE = ePlus univ + eMinus univ`; deficit domination is derived.
- Structural equivalence uses finite attainment. W1, W2 and W3 use explicit primitive-component worlds and actual `selectedRadius` geometry.
- Split estimation is a deterministic finite weighted-expectation analogue. It proves the fold-A-selected contrast target from unbiased finite evaluation means; it does not literally formalize sigma algebras/conditional independence and does not claim unbiasedness for true range.
- Continuous Neyman interior optimality assumes positive coefficients. A zero coefficient gives a zero closed-boundary allocation; under strict `n_a > 0`, this is an infimum/boundary issue.
- Chain D assumes finite nonempty per-relation action type, the full Cartesian joint-action space, explicit nonnegative normalized product weights, and a uniform mixed-difference bound. The active D6 route defines actual minimax scalar compression loss, proves its 1-Lipschitz bridge, proves `L_Aq(S)=sum_{j notin S} C_j/2`, and derives surrogate optimality from actual Top-C. D6 accepts neither an arbitrary objective-perturbation assumption nor an external surrogate-optimality assumption.
- A8 uses gauge error `osc(Qhat-Q)/2`; it does not use aggregate RMSE or response-surface Spearman.
- A9/A10 require strict between-relation capacity margins. Within-relation extrema gaps and between-relation ranking gaps are different objects.
- A11 is an exact two-relation, two-action witness: every local argmax/argmin and local phase condition is stable while global capacity order reverses.
- The `1/2` sharpness theorem is a constructive worst-case Fin-2 family. It establishes that no smaller universal constant can replace the boundary; it does not claim the same adversarial construction for every arbitrary fixed response surface without an attained-gap assumption.
- Tie-impossibility theorems are constructive Fin-2 uniform-radius counterexamples, for within-relation extrema and between-relation Top-1 membership.
- A12 now includes the implementation-matched zero-sum assumption `sum w = 0`, derives the exact gauge bound `|Dhat-D| ≤ ||w||_1 delta_Q^circ`, and proves `lambdaD < 1` sign stability when `D ≠ 0`. It does not imply accurate magnitude ranking of D across relations.
- The Operational MASTER is frozen and unchanged. No auxiliary/V6 theorem modifies or invalidates its assumptions.
